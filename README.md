this is my learning setup
in first i learn various cmd like git init the git add . after this i commit my file then check git status or git branch -> and then git remote add origin"repo link"
->to check this type git remote -v --> after git push -u origin "branch name "
now in daily  type only git add .
git commit -m "message"
git push
